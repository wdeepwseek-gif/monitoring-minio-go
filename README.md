# 🪣 MinIO Telegram Monitor

Мощный мониторинг событий MinIO с уведомлениями в Telegram. Отслеживайте изменения в ваших бакетах в реальном времени!

## 📖 Описание

Сервис мониторинга MinIO,который отслеживает события в бакетах (создание/удаление файлов) и отправляет уведомления в Telegram. Подходит для мониторинга хранилища, аудита операций и командной работы. Бот подключается к MinIO через API,фильтрует события по настройкам и автоматически распределяет уведомления по топикам в зависимости от структуры папок.

## ✨ Возможности

- 🔔 **Реальное время**: Мгновенные уведомления о событиях MinIO
- 🎯 **Умная фильтрация**: Настраиваемые фильтры по типу событий, размеру файлов и расширениям
- 🏷️ **Топики**: Автоматическое определение топиков по структуре папок
- 📱 **Красивые уведомления**: Современный дизайн сообщений с эмодзи
- ⏱️ **Очередь Telegram**: Отправка каждые 9 секунд и до 5 повторов с задержкой 1 минута
- 🧾 **Дневные логи**: Автоматическая ротация файлов и хранение истории 30 дней
- 🛡️ **Безопасность**: Правильное экранирование символов Markdown
- 🐳 **Docker Ready**: Готовый к продакшену Dockerfile

## 🚀 Быстрый старт

### 1. Настройка конфигурации

Создайте файл `config.yaml` (используйте `config.example.yaml` как шаблон):

```yaml
minio:
  endpoint: "your-minio-server:9000"
  accessKey: "your-access-key"
  secretKey: "your-secret-key"
  useSSL: false
  bucketToWatch: "your-bucket-name"

telegram:
  botToken: "your-telegram-bot-token"

chats:
  - id: -1001234567890
    topic_id: 12345
    label: "devops"

topics:
  xwiki: 12345
  backup: 12346
  logs: 12347
  default: 12345

notifications:
  enabledEvents:
    - "s3:ObjectCreated:*"
    - "s3:ObjectRemoved:*"
  excludeExtensions:
    - ".tmp"
    - ".log"
  minSizeBytes: 0
  maxSizeBytes: 1073741824
```

### 2. Запуск через Docker

```bash
# Сборка образа
docker build -t minio-telegram-monitor .

# Запуск контейнера с переменными окружения (рекомендуется)
docker run -d \
  --name minio-monitor \
  -e MINIO_ACCESS_KEY="your-access-key" \
  -e MINIO_SECRET_KEY="your-secret-key" \
  -e MINIO_ENDPOINT="your-minio-server:9000" \
  -e MINIO_BUCKET="your-bucket-name" \
  -e TELEGRAM_BOT_TOKEN="your-telegram-bot-token" \
  -v $(pwd)/config.yaml:/app/config.yaml \
  minio-telegram-monitor

# Или с использованием файла конфигурации (менее безопасно)
docker run -d \
  --name minio-monitor \
  -v $(pwd)/config.yaml:/app/config.yaml \
  minio-telegram-monitor
```

### 3. Запуск напрямую

```bash
# Установка зависимостей
go mod download

# Запуск
go run main.go
```

## 📱 Пример уведомления

```
✅ *Файл добавлен*

🏷️ *Топик:* #xwiki
📦 *Bucket:* developer
📄 *Object:* xwiki/28-07-2025/manage-offline-soft-distrib.sh
📏 *Size:* 1.2 КБ
🕒 *Time:* 2025-01-30 13:03:18
🔔 *Type:* ObjectCreated
```

## ⚙️ Конфигурация

### MinIO настройки
- `endpoint` - Адрес MinIO сервера (или переменная `MINIO_ENDPOINT`)
- `accessKey` - Ключ доступа (или переменная `MINIO_ACCESS_KEY`)
- `secretKey` - Секретный ключ (или переменная `MINIO_SECRET_KEY`)
- `useSSL` - Использовать SSL
- `bucketToWatch` - Имя бакета для мониторинга (или переменная `MINIO_BUCKET`)

### Telegram настройки
- `botToken` - Токен Telegram бота (или переменная `TELEGRAM_BOT_TOKEN`)
- `chats` - Массив чатов для уведомлений
- `topics` - Маппинг топиков по названиям

### Фильтры уведомлений
- `enabledEvents` - События для отслеживания
- `excludeExtensions` - Исключить файлы с расширениями
- `minSizeBytes` - Минимальный размер файла
- `maxSizeBytes` - Максимальный размер файла

## 🔒 Безопасность

### ⚠️ Важно: защита чувствительных данных

1. **Файл `config.yaml` не должен попадать в репозиторий**
   - Он уже добавлен в `.gitignore`
   - Создайте его из `config.example.yaml` локально

2. **Используйте переменные окружения для продакшена**
   - Переменные окружения автоматически переопределяют значения из `config.yaml`
   - Это более безопасный способ хранения паролей и токенов

3. **Поддерживаемые переменные окружения:**
   ```bash
   export MINIO_ACCESS_KEY="your-access-key"
   export MINIO_SECRET_KEY="your-secret-key"
   export MINIO_ENDPOINT="your-minio-server:9000"
   export MINIO_BUCKET="your-bucket-name"
   export TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
   ```

4. **Приоритет конфигурации:**
   - Переменные окружения (наивысший приоритет)
   - Файл `config.yaml` (низший приоритет)

5. **Пример запуска с переменными окружения:**
   ```bash
   MINIO_ACCESS_KEY="key" \
   MINIO_SECRET_KEY="secret" \
   MINIO_ENDPOINT="localhost:9000" \
   MINIO_BUCKET="my-bucket" \
   TELEGRAM_BOT_TOKEN="token" \
   go run main.go
   ```

## 🐳 Docker

Dockerfile использует multi-stage сборку для оптимизации размера образа и безопасности.

## 🧾 Логирование

- Логи сохраняются в каталоге `logs/`
- Имя файла соответствует дате: `YYYY-MM-DD.log`
- Ротация выполняется ежедневно, старше 30 дней автоматически удаляются
- Все сообщения дополнительно дублируются в stdout для удобного мониторинга

## ⏱️ Очередь уведомлений

- Внутренняя очередь защищает Telegram API от перегрузки
- Отправка сообщений выполняется с интервалом `9` секунд
- При ошибках выполняется до `5` повторов с задержкой `1` минута
- Все попытки логируются, что упрощает диагностику

## 🔧 Разработка

### Требования
- Go 1.23+
- MinIO Server
- Telegram Bot Token

### Локальная разработка
```bash
go mod download
go run main.go
```

---

**Сделано с ❤️ для DevOps команды**
